version: '3.8'
services:
  portfolio:
    build: .
    container_name: claudev-portfolio
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
